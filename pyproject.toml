[project]
name = "Chew_or_Die"
version = "0.1.0"
description = "A Pyxel-based retro arcade game combining snake game mechanics with a Julia set fractal mini-game"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyxel>=2.5.10",
]

[project.scripts]
chew_or_die = "main:main"
